const PageError = () => {
    return (
        <div className="flex items-center justify-center h-full">
            <p className="text-red-500 text-lg">An error occurred while loading the page.</p>
        </div>
    );
};

export default PageError;
